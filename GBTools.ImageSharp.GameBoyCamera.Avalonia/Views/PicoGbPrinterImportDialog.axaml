<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:GBTools.ImageSharp.GameBoyCamera.Avalonia.ViewModels"
        xmlns:models="using:GBTools.ImageSharp.GameBoyCamera.Avalonia.Models"
        x:Class="GBTools.ImageSharp.GameBoyCamera.Avalonia.Views.PicoGbPrinterImportDialog"
        x:DataType="vm:PicoGbPrinterImportDialogViewModel"
        Width="760"
        Height="520"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Title="Import From Pico GB Serial">
    <Grid RowDefinitions="*,Auto" Margin="20">
        <StackPanel Spacing="14">
            <TextBlock Text="Choose how to import a capture from the attached Pico GB Printer serial device."
                       FontSize="16"
                       FontWeight="SemiBold" />
            <TextBlock Text="{Binding PortSummary}"
                       TextWrapping="Wrap"
                       Opacity="0.82" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#FFD8A7A7"
                       TextWrapping="Wrap"
                       IsVisible="{Binding HasError}" />

            <StackPanel Spacing="8">
                <TextBlock Text="Serial port"
                           FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding AvailablePorts}"
                          SelectedItem="{Binding SelectedPort, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="models:PicoGbPrinterPortOption">
                            <StackPanel Spacing="2" Margin="0,4">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Details}"
                                           FontSize="12"
                                           Opacity="0.78"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="Capture source"
                           FontWeight="SemiBold" />
                <ComboBox ItemsSource="{Binding AvailableModes}"
                          SelectedItem="{Binding SelectedMode, Mode=TwoWay}" />
                <TextBlock Text="{Binding ModeSummary}"
                           TextWrapping="Wrap"
                           Opacity="0.8" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    MinWidth="90" />
            <Button Content="Import"
                    Command="{Binding ConfirmCommand}"
                    MinWidth="100" />
        </StackPanel>
    </Grid>
</Window>