name: Publish PicNRec Web Serial Library

on:
  push:
    branches:
      - master
      - develop
    paths:
      - .github/workflows/publish-picnrec-web-serial.yml
      - picnrec-web-serial/**
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish-library:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./picnrec-web-serial
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: picnrec-web-serial/package-lock.json
          registry-url: https://npm.pkg.github.com
          scope: '@maddoscientisto'

      - name: Install workspace dependencies
        run: npm ci

      - name: Build library package
        run: npm run build:lib

      - name: Publish library package
        run: npm publish --workspace @maddoscientisto/picnrec-web-serial
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}